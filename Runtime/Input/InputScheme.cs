namespace UyiCore.Input
{
    /// <summary>Kiểu thiết bị đang dùng gần nhất — để đổi icon phím gợi ý theo tay cầm/chuột-phím/cảm ứng.</summary>
    public enum InputScheme
    {
        KeyboardMouse,
        Gamepad,
        Touch,
    }
}
